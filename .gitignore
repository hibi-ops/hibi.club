# dependencies
/node_modules

# next.js build output
/.next
/.next-prod
/out
/build

# production
/dist

# environment variables
.env*
!.env.example

# misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea

# deploy
.vercel

# local archive of the pre-rebuild site: 126MB of binaries (point clouds, TTFs)
# that would live in git history forever. Kept on disk, out of the repo.
_archive/
.env.local

# Finder duplicates ("Foo 2.tsx"). They are byte copies that drift out of sync
# and were committed twice by a broad `git add`.
* 2.*
