# Hibi website — working notes

纯静态导出（`output:'export'`）双语营销站。`npm run dev` 起本地，`npm run build` 出 `out/`。

## Design invariants — 用户点名保留，改版前先问
- **整行反色 highlight**：hover 文字变墨色硬底白字，零过渡。落在 `.hero-title .line:hover`
  与 `.hl:hover`（全站 section 标题）。用户原话："超喜欢这个，多应用记住要保留"。
- 空心描边 = 缺席：`$0`、`.wi-ord`、头奖时的 `$68.00`、大 marquee、日々署名共用此语义。
- 大 marquee 用 FoodDelicious（拉丁）+ CJK 回退（日々）；全站仅此一条 + 顶部数据 ticker，不加第三条滚动物。
- **等高线场**（Wash.tsx）：用户点名保留——"非常好这个等高线"。单色墨线、发丝、每五条一根
  计曲线、指针处地面隆起、CSS .lit 为回退。它成立的原因是**和全站同一支笔**（规线／套准十字／
  描边字／空心零），并呼应产品的街区语义。
- **不许再发明蓝色**。试过两支，都被否，原因不同，记下来别再试：
  - `#0b5cff` 色相 220°，wordmark 是 196.8°，差 23 度 → **是另一个颜色，不是更亮的**。
    用户："太冷了看上去毫无关联"。
  - `#00a1e0` 色相对（196.9°），但饱和度从 67% 开到 100% → 用户："好丑"。
    `#52b6dd` 好看正是因为里面还有灰；开满就成了默认链接色。而且**色块越大能承受的
    彩度越低**——26px 上"脆"的颜色，铺 600px 就震。
  结论：**音量靠面积，不靠彩度。** 全站只有 `--sky #52b6dd` 一支蓝，
  作**字**时是信息（结算卡金额、wordmark 面），作**底**时是标记（墨字压它 7.95:1）。
  `--brandD/--brandInk/--brandP` 保持 DESIGN.md §5.1 原值，它们从来没出过问题。
- **Y2K 只取 1-bit 那一半**：抖动网点、点阵、终端读数。铬合金／彩虹全息／aqua 高光／
  闪星是被否过的"太古早"那一半，不碰。判据同 three.js：**有职务 + 同笔触**。
  已落地：墨色整版的上下缘是**网点溶解**（6 行 10px 点阵，覆盖率 92→3%，白点缩进黑底），
  意思是这块黑版是"印"上去的。它是 gradient 的反面——每个点都是硬边（色标只走 0.1px），
  斜坡由可数的级构成，不是平滑。间距必须 ≥10px：7px 时点不再是点，变成噪点。
  **position 必须写在 `background-position` 里**——`radial-gradient(...) left 0 top 0`
  是 `background` 简写语法，会静默作废整条 `background-image`。
  版面要给溶解留位置：`.tone-ink` 上下各加 60px padding，否则首个 label 陷在点阵里。
  **试过并否掉**：把细网格裁进 37px 的 section 编号（`background-clip:text`）——读起来是
  渲染质量差，不是 LED 显示屏。真点阵是大点少点（5×7），细网格裁字形只能得到发虚的字。
- **费率数字的面积 = 费率**，所以高度走 √值（眼睛比的是面积不是高度）。`--rw` 由
  `PriceTiers` 从文案里算出来，费率改了字号会跟着改，不会留下一段说谎的排版。
  **768px 以下整个关掉编码，不要用 max() 兜底**——兜底把 8% 和 4% 压成 29/28px，
  不只是丢了编码，是让两档看起来一样。而且手机上行是堆叠的，三个数字不在同一眼里。
- **表单是"划线"不是"盒子"**：单根 1.5px 规线当书写线，没有框。这是全站最后一处还在
  画容器的地方。下划线字段的已知代价是可点性弱，用三样补：规线 1.5px／22% 而不是发丝、
  输入框保留 36/44px 全高（热区没变）、hover 给整格上底色。focus 是硬切：2px 满墨 + 标签转墨。
- **整屏铺开的那块用 `tone-ink`，不用彩色**。600px 的纯色场必须自己站得住，墨色天然站得住。
  首页只有"三方，同一笔交易"用它——一页需要 hero 之后的第二个锚点。
- **顶栏保持浅色毛玻璃**。做过墨色实心 masthead，用户否掉了。
- **入场是扫，不是淡入**：`.rise` 用 clip-path 左→右揭开，全程不透明度不变，对齐全站硬切
  语汇。必须 `backwards` 不能 `both`——`both` 会把 clip-path 永久留在元素上，
  `.cta-row` 里按钮的 focus ring 会被裁掉。
- **顶栏是墨色实心 masthead**，不是毛玻璃（globals.css 头部第一行规则就写着 no glass）。
  不加 blur：底下内容滚过去时颜色会变，会闪的横条不叫 masthead。
- **彩色流体渐变已被否决——"太古早"**：2019 的语言，和本站描边/hairline/硬切语汇冲突。
  three.js 判据 = **"有职务 + 同笔触"，两条缺一不可**。历史拆除：隐形点阵场、开盒粒子、
  线框日々展品、彩色 wash。
- 自定义光标 `mix-blend-difference`；触屏 / reduced-motion 不启动。
- **按钮 hover 禁止位移**（磁性已按用户要求拆除，"太 ai"）。hover 只变色/变底；箭头在钮内滑动可以。
- `.field` 类名属于表单字段。任何全屏定位层严禁再用这个名字（撞名事故记录在 globals.css 注释）。

## 结构速查
- 文案：`content/{en,zh}.ts`，结构 `content/types.ts`。中文文案受外部禁词表约束，改前问。
- 字体：Noto Sans SC 自托管分片（`scripts/fetch-noto-sc.sh` 再生）；FoodDelicious 只做 display。
- 交互组件：LedgerLive（活账本）、WalkIn（可玩核销，真概率盲盒）、Estimator（?cap= 传进表单）、
  WeekStats（听 `hibi:redeem/reset` 事件）、Cursor、Pointer（磁性+日々视差+sheen）。
